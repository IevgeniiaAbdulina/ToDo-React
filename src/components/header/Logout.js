import React from "react";

import "./Logout.css";

class Logout extends React.Component {
  state = {
    _id: ""
  };

  render() {
    return (
      <div>
        <div>Log Out</div>
      </div>
    );
  }
}

export default Logout;
